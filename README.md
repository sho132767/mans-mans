# 使い方1
#include "mbed.h"
#include "servo.hpp"
#include "config.hpp"
Timer timer;
int main() {
Servo servo（pin)
    servo.set_angle(0);
    while(true){
    while(timer.read() - t < dt){
        t=timer.read();
    }
    }
   }
   
   
